# 프로젝트 규칙

전체 명세는 SPEC.md 참조. 작업 전에 해당 절을 읽을 것.

- Phase 0에는 네트워크 코드를 넣지 않는다. Retrofit, Ktor, INTERNET 권한 금지.
- SPEC.md 4절의 스키마는 변경하지 않는다. 필드가 불필요해 보여도 그대로 둔다.
- 의존성은 버전 카탈로그(libs.versions.toml) 방식으로만 추가한다.
- `!!` 연산자를 쓰지 않는다.
- 한 번에 한 작업(T번호)만 수행한다. 다음 작업으로 넘어가지 않는다.
